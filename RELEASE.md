# Release Notes

## v1.5.0

- Added skeleton loaders for Favorites and Settings tabs (matching `StreamCard`/`StreamerCard` layouts with a shimmer animation) instead of the generic spinner
- Reworked `AppLoader` into a Twitch sign-in animation, shown only while waiting on the Twitch OAuth flow
