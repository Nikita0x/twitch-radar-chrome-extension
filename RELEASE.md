# Release Notes

## v1.5.0

- released the extension for Firefox
- added an Ideas tab to the Changelog screen
- added a real uninstall feedback survey (`browser.runtime.setUninstallURL`, Chrome + Firefox) with a hosted Privacy Policy page
- fixed a CORS bug that silently dropped feedback submissions from the published (Chrome Web Store) build
- updated Firefox data collection permissions disclosure to accurately reflect the uninstall survey
